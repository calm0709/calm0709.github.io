```
# string을 split 할 때는 공백을 기준으로 나눈다
s = "apple pie"
newS = a.split()
print(newS)      #["apple", "pie"]

# 공백이 없을 때는 list를 만들어서 나눈다
c = cookey
l = list()
for i in range(len(c)): 
  l += c[i]
print(l)        #["c", "o", "o", "k", "e", "y"]
```
